/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: { domains: ['graph.facebook.com', 'cdninstagram.com', 'scontent.cdninstagram.com'] }
};
module.exports = nextConfig;
